istros falsos.</button><button class="choice" data-sol="0">Los aliados del Este organizaron el sabotaje.</button><button class="choice" data-sol="0">Todo fue un error casual sin coordinación.</button></div>`);setTimeout(()=>$$('[data-sol]').forEach(b=>b.onclick=()=>{if(b.dataset.sol==='1'){sv.done[m.id]=3;sv.u=Math.max(sv.u,Math.min(20,m.id+1));sv.cmd+=3;sv.st=Object.values(sv.done).reduce((a,b)=>a+b,0);save();$('#modal').classList.remove('on');campaign();guide('Expediente resuelto. La investigación avanza.',true)}else guide('Esa conclusión no coincide con las pistas. Inténtalo otra vez.')}),0)}
function showCards(){modal(`<div class="ey">MIS CARTAS</div><h2>Consejo de Sangre de Luna</h2><div class="grid">${cards.filter(c=>!['mapa','archivo','murallas'].includes(c.id)).map(c=>`<div class="tile"><img src="${A+c.img}"><div><b>${c.n}</b><small>${Object.entries(c.g).map(([k,v])=>I[k]+' +'+v).join(' · ')}</small></div></div>`).join('')}</div>`)}function upgrades(){modal(`<div class="ey">MEJORAR CIUDADELA</div><h2>Mando disponible: ${sv.cmd}</h2><p>Este sistema es opcional. Por ahora, acumula Mando completando misiones; las mejoras avanzadas se desbloquearán progresivamente.</p>`)}function all(){modal(`<div class="ey">CAMPAÑA COMPLETA</div><h2>20 misiones</h2><div class="grid">${missions.map(m=>`<div class="tile" style="opacity:${m.id<=sv.u?1:.35}"><img src="${A+m.img}"><div><small>MISIÓN ${m.id}</small><b>${m.n}</b><div>${'★'.repeat(sv.done[m.id]||0)}${'☆'.repeat(3-(sv.done[m.id]||0))}</div></div></div>`).join('')}</div>`)}function help(){modal('<div class="ey">CÓMO JUGAR</div><h2>Solo tres pasos</h2><p>1. Responde al evento. 2. Juega cartas para completar los objetivos. 3. Termina la ronda. Si te pierdes, pulsa 🐺 y Lykos te dirá qué hacer.</p>')}
const tut=[['Bienvenido','Yo soy Lykos. Te acompañaré durante toda la campaña.'],['Paso 1','Cada ronda empieza con un evento. Primero debes tomar una decisión.'],['Paso 2','Después juega cartas. Cada carta aumenta Información, Lealtad, Estrategia o Defensa.'],['Paso 3','Cuando termines, pulsa TERMINAR RONDA. Eso es todo lo necesario para comenzar.']];let ti=0;function training(){ti=0;$('#tutorial').classList.add('on');showTut()}function showTut(){$('#tt').textContent=tut[ti][0];$('#tx').textContent=tut[ti][1];$('#tn').textContent=ti===tut.length-1?'ENTRAR A LA CAMPAÑA':'SIGUIENTE';guide(tut[ti][1],ti===0)}$('#tn').onclick=()=>{if(ti<tut.length-1){ti++;showTut()}else{$('#tutorial').classList.remove('on');sv.tut=true;save();campaign()}};
$('#new').onclick=()=>{sv={u:1,done:{},st:0,cmd:0,tut:false};save();training()};$('#cont').onclick=()=>sv.tut?campaign():training();$('#cardsBtn').onclick=showCards;$('#cardsBtn2').onclick=showCards;$('#upgradeBtn').onclick=upgrades;$('#allBtn').onclick=all;$('#helpBtn').onclick=help;$('#home').onclick=()=>scr('menu');setTimeout(()=>guide('Yo soy Lykos. Pulsa Nueva Partida y entrenaremos juntos.',true),600);
</script></body></html>
