extContent=voiceOn?'◉ LYKOS':'○ LYKOS'};$('#music').onclick=()=>{musicOn=!musicOn;let a=$('#bg');a.volume=.2;musicOn?a.play().catch(()=>{}):a.pause();$('#music').textContent=musicOn?'♫ MÚSICA ON':'♫ MÚSICA'};
function campaign(){scr('campaign');$('#prog').textContent=Object.keys(sv.done).length;$('#stars').textContent=sv.st;$('#cmd').textContent=sv.cmd;let m=missions[Math.min(19,sv.u-1)],next=missions[m.id]||null;$('#era').textContent=['ERA DE VIGILANCIA','ERA DE ALIANZAS','ERA DEL CONSEJO','ERA DEL UMBRAL'][m.a-1];$('#currentMission').innerHTML=`<img src="${A+m.img}"><div class="mcopy"><small>AHORA · MISIÓN ${m.id} DE 20</small><b>${m.n}</b><p>${m.ded?'Usa las pistas reunidas para resolver el expediente.':'Responde eventos y juega cartas para completar los objetivos.'}</p></div>`;$('#currentMission').onclick=()=>m.ded?dossier(m):start(m);$('#nextMission').innerHTML=next?`<small>DESPUÉS</small><b>${next.n}</b><div>Se desbloquea al completar la misión actual.</div>`:'Has llegado al final de la campaña.';guide('Juega únicamente la misión marcada como AHORA. Lo demás puede esperar.')}function card(c){return `<div class="card" data-c="${c.id}"><img src="${A+c.img}"><span class="cost">${c.c}</span><div class="copy"><small>CARTA</small><b>${c.n}</b><p>${Object.entries(c.g).map(([k,v])=>I[k]+' +'+v).join(' · ')}</p></div></div>`}
function start(m){S={m,rd:1,ap:3,v:{i:0,l:0,s:0,d:0},th:0,h:sh(cards.map(c=>c.id)).slice(0,6),p:[],e:events[Math.floor(Math.random()*events.length)],choice:false,log:[]};scr('game');render();guide('Paso uno: responde al evento de la derecha. Después jugarás tus cartas.',m.id===1)}function render(){let f=focus();$('#mk').textContent=`ACTO ${S.m.a} · MISIÓN ${S.m.id}`;$('#mn').textContent=S.m.n;$('#round').textContent=S.rd;$('#rmax').textContent=S.m.r;$('#ap').textContent=S.ap;$('#mart').src=A+S.m.img;$('#goals').innerHTML=Object.entries(S.m.g).map(([k,g])=>`<div class="goal ${S.v[k]>=g?'done':''} ${k===f?'focus':''}"><span>${S.v[k]>=g?'✓':I[k]} ${N[k]}</span><b>${Math.min(S.v[k],g)}/${g}</b></div>`).join('');$('#threat').textContent='⚠ Amenaza '+S.th+'/10';let e=S.e;$('#event').innerHTML=`<img src="${A+e.img}"><div><small>EVENTO</small><b>${e.n}</b><p>${e.t}</p></div>`;$('#choices').innerHTML=S.choice?'<div class="goal">Decisión registrada.</div>':e.ch.map((x,i)=>`<button class="choice" data-x="${i}"><b>${x[0]}</b><div>${Object.entries(x[1]).map(([k,v])=>I[k]+' +'+v).join(' · ')}</div></button>`).join('');$$('[data-x]').forEach(b=>b.onclick=()=>{gain(e.ch[+b.dataset.x][1]);S.choice=true;S.log.unshift('Decisión: '+e.ch[+b.dataset.x][0]);render();guide('Bien. Ahora juega cartas que aumenten los recursos que todavía te faltan.')});$('#hand').innerHTML=S.h.map(id=>card(cards.find(c=>c.id===id))).join('');$$('[data-c]').forEach(el=>el.onclick=()=>play(el.dataset.c));$('#played').innerHTML=S.p.slice(-4).map(id=>{let c=cards.find(x=>x.id===id);return `<div class="played"><img src="${A+c.img}"><span>${c.n}</span></div>`}).join('');$('#prompt').textContent=S.choice?'Juega tus cartas y luego termina la ronda.':'¿Cómo responderás al evento?';$('#log').innerHTML=S.log.map(x=>'• '+x).join('<br>');let st=!S.choice?1:(S.ap>0&&!met()?2:3);$('#guide').innerHTML=`<div class="step">${st}/3</div><div><div class="ey">AHORA HAZ ESTO</div><b>${st===1?'Responde al evento':st===2?'Juega tus cartas':'Termina la ronda'}</b><p>${st===1?'Elige una de las dos decisiones.':st===2?(f?'Busca cartas con '+I[f]+' para subir '+N[f]+'.':'Juega una carta útil.'):'Pulsa TERMINAR RONDA para avanzar.'}</p></div>`;if(met())win()}
function play(id){let c=cards.find(x=>x.id===id);if(!S.choice)return guide('Primero responde al evento. Después podrás jugar cartas.',true);if(c.c>S.ap)return guide('No tienes suficientes acciones para esa carta.');S.ap-=c.c;S.h.splice(S.h.indexOf(id),1);S.p.push(id);gain(c.g);if(id==='kai'||id==='ella')S.th=Math.max(0,S.th-1);S.log.unshift('Carta: '+c.n);render()}$('#end').onclick=()=>{if(!S.choice)return guide('Primero responde al evento.');if(met())return win();if(S.rd>=S.m.r)return lose();S.rd++;S.ap=3;S.p=[];S.h=sh(cards.map(c=>c.id)).slice(0,6);S.th++;S.e=events[Math.floor(Math.random()*events.length)];S.choice=false;render();guide('Nueva ronda. Primero responde al evento.')};function win(){let s=1+(S.th<=4)+(S.rd<S.m.r);sv.done[S.m.id]=Math.max(sv.done[S.m.id]||0,s);sv.u=Math.max(sv.u,Math.min(20,S.m.id+1));sv.cmd+=s;sv.st=Object.values(sv.done).reduce((a,b)=>a+b,0);save();modal(`<h2>Misión completada</h2><p>${S.m.n} · ${'★'.repeat(s)}</p><button id="back" class="btn gold">VOLVER A CAMPAÑA</button>`);setTimeout(()=>$('#back').onclick=()=>{$('#modal').classList.remove('on');campaign()},0)}function lose(){modal('<h2>Operación interrumpida</h2><p>Se agotaron las rondas.</p><button id="retry" class="btn">REINTENTAR</button>');setTimeout(()=>$('#retry').onclick=()=>{$('#modal').classList.remove('on');start(S.m)},0)}
function dossier(m){modal(`<div class="ey">EXPEDIENTE DE DEDUCCIÓN</div><h2>${m.n}</h2><p>Lykos: elige la conclusión que mejor encaja con lo que has visto en las misiones anteriores.</p><div class="choices"><button class="choice" data-sol="1">La ruta fue manipulada mediante señales y reg