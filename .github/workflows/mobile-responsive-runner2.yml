name: Mobile responsive patch runner 2

on:
  push:
    branches: ["main"]
    paths:
      - ".github/mobile-patch-trigger-2"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Execute responsive patch source
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          source = Path('.github/workflows/one-time-mobile-responsive-game.yml').read_text(encoding='utf-8')
          token = "python - <<'PY'\n"
          start = source.index(token) + len(token)
          end = source.index("\n          PY", start)
          block = source[start:end]
          code = '\n'.join(line[10:] if line.startswith('          ') else line for line in block.splitlines())
          exec(compile(code, 'mobile-responsive-patch', 'exec'))
          PY

      - name: Commit responsive game patch
        shell: bash
        run: |
          git config user.name "sangre-de-luna-bot"
          git config user.email "actions@users.noreply.github.com"
          git rm -f .github/workflows/one-time-mobile-responsive-game.yml .github/workflows/mobile-responsive-runner.yml .github/workflows/mobile-responsive-runner2.yml .github/mobile-patch-trigger .github/mobile-patch-trigger-2
          git add game-runtime/live-loader.js
          git commit -m "Make game automatically responsive on mobile screens"
          git push origin HEAD:main
