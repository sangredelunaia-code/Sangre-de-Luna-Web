name: Guardian Wolf Check

on:
  push:
    branches:
      - feature/guardian-wolf-assistant

permissions:
  contents: read

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check guardian-wolf.js
          node --check cronista-global.js
          node --check desafios-admin.js
          node --check api/cronista.js
